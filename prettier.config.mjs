/**
 * @see https://prettier.io/docs/configuration
 * @type {import('prettier').Config}
 */
const config = {
    bracketSpacing: true,
    semi: true,
    singleQuote: true,
    tabWidth: 4,
    trailingComma: 'es5',
};

export default config;
